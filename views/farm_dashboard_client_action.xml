<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action for Farm Dashboard -->
    <record id="action_farm_dashboard" model="ir.actions.client">
        <field name="name">Farm Dashboard</field>
        <field name="tag">farm_management.dashboard</field>
    </record>
    
    <!-- Dashboard Menu Item (replaces the previous one) -->
    <menuitem id="menu_farm_dashboard"
              name="Dashboard"
              parent="menu_farm_root"
              action="action_farm_dashboard"
              sequence="1"/>
</odoo>
